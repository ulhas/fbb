import SwiftUI

struct CommunityView: View {
    var body: some View {
        ComingSoonScaffold(
            symbol: "person.3.fill",
            title: "Community",
            subtitle: "Train with everyone on Persist. Leaderboards, partner sessions, and weekly check-ins are on the way."
        )
    }
}

#Preview { CommunityView() }
